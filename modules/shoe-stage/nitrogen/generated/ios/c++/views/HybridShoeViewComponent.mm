///
/// HybridShoeViewComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#import "HybridShoeViewComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridShoeViewSpecSwift.hpp"
#import "ShoeStage-Swift-Cxx-Umbrella.hpp"

#if __has_include(<cxxreact/ReactNativeVersion.h>)
#include <cxxreact/ReactNativeVersion.h>
#if REACT_NATIVE_VERSION_MINOR >= 82
#define ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
#endif
#endif

using namespace facebook;
using namespace margelo::nitro::shoestage;
using namespace margelo::nitro::shoestage::views;

/**
 * Represents the React Native View holder for the Nitro "ShoeView" HybridView.
 */
@interface HybridShoeViewComponent: RCTViewComponentView
+ (BOOL)shouldBeRecycled;
@end

@implementation HybridShoeViewComponent {
  std::shared_ptr<HybridShoeViewSpecSwift> _hybridView;
  BOOL _didDropView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridShoeViewComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridShoeViewComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    _props = HybridShoeViewShadowNode::defaultSharedProps();
    std::shared_ptr<HybridShoeViewSpec> hybridView = ShoeStage::ShoeStageAutolinking::createShoeView();
    _hybridView = std::dynamic_pointer_cast<HybridShoeViewSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  ShoeStage::HybridShoeViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) notifyOnDropView {
  // A recycled component can later be invalidated. Notify only once per mount.
  if (_didDropView) {
    return;
  }
  ShoeStage::HybridShoeViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.onDropView();
  _didDropView = YES;
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // A props update marks a newly mounted or still-active component.
  _didDropView = NO;

  // 1. Downcast props
  const auto& newViewProps = *std::static_pointer_cast<const HybridShoeViewProps>(props);
  const auto* oldViewProps = static_cast<const HybridShoeViewProps*>(oldProps.get());
  ShoeStage::HybridShoeViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update only props that differ from the previous Props snapshot.
  const bool hasTransactionPropChanges = oldViewProps == nullptr
      ? newViewProps.hasAnyProvidedProps()
      : !newViewProps.hasSameProps(*oldViewProps);
  if (hasTransactionPropChanges) {
    swiftPart.beforeUpdate();

    // model: string
    if (oldViewProps == nullptr
          ? newViewProps.model.isProvided()
          : !newViewProps.model.hasSameValue(oldViewProps->model)) {
      swiftPart.setModel(newViewProps.model.get());
    }
    // verticalFovDegrees: number
    if (oldViewProps == nullptr
          ? newViewProps.verticalFovDegrees.isProvided()
          : !newViewProps.verticalFovDegrees.hasSameValue(oldViewProps->verticalFovDegrees)) {
      swiftPart.setVerticalFovDegrees(newViewProps.verticalFovDegrees.get());
    }
    // shoes: array
    if (oldViewProps == nullptr
          ? newViewProps.shoes.isProvided()
          : !newViewProps.shoes.hasSameValue(oldViewProps->shoes)) {
      swiftPart.setShoes(newViewProps.shoes.get());
    }
    // legMatte: boolean
    if (oldViewProps == nullptr
          ? newViewProps.legMatte.isProvided()
          : !newViewProps.legMatte.hasSameValue(oldViewProps->legMatte)) {
      swiftPart.setLegMatte(newViewProps.legMatte.get());
    }
    // matchCamera: boolean
    if (oldViewProps == nullptr
          ? newViewProps.matchCamera.isProvided()
          : !newViewProps.matchCamera.hasSameValue(oldViewProps->matchCamera)) {
      swiftPart.setMatchCamera(newViewProps.matchCamera.get());
    }

    // Update hybridRef if it changed
    if (oldViewProps == nullptr
          ? newViewProps.hybridRef.isProvided()
          : !newViewProps.hybridRef.hasSameValue(oldViewProps->hybridRef)) {
      // hybridRef changed - call it with new this
      const auto& maybeFunc = newViewProps.hybridRef.get();
      if (maybeFunc.has_value()) {
        maybeFunc.value()(_hybridView);
      }
    }

    swiftPart.afterUpdate();
  }

  // 3. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

+ (BOOL)shouldBeRecycled {
  return ShoeStage::ShoeStageAutolinking::isShoeViewRecyclable();
}

- (void)prepareForRecycle {
  [self notifyOnDropView];
  [super prepareForRecycle];
  ShoeStage::HybridShoeViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.maybePrepareForRecycle();
}

#ifdef ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
- (void)invalidate {
  [self notifyOnDropView];
  [super invalidate];
}
#endif

@end
