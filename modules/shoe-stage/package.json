{
  "name": "react-native-shoe-stage",
  "version": "0.0.1",
  "private": true,
  "description": "RealityKit view that draws shoe models at given poses over the camera preview, and the device gravity for posing them",
  "main": "src/index.ts",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "nitrogen --logLevel=warning"
  },
  "peerDependencies": {
    "react-native": "*",
    "react-native-nitro-modules": "*"
  }
}
