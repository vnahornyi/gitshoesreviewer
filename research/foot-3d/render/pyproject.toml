[project]
name = "foot-3d-render"
version = "0.0.1"
description = "Synthetic foot renders with Blender as a Python module (bpy), no Blender UI"
requires-python = ">=3.13,<3.14"
dependencies = [
  "bpy==5.2.2",
  "numpy",
]

[tool.uv]
package = false
